requests>=2.28.0
beautifulsoup4>=4.11.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
sentence-transformers>=2.2.2
joblib>=1.3.0
biopython>=1.81
lxml>=4.9.0
paddlepaddle>=2.6.0  # 如果使用Paddle模型部署
paddlenlp>=2.5.0     # 可选，用于中文或生物医学文本处理  
# paddlelp 这个库好像很大，挺难装的
# 也有可能jieba这个库（以及它底层的某些依赖）目前对 Python 3.13 的官方预编译包支持还不完善，导致 pip找不到合适的版本来安装